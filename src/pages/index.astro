---
import PublicLayout from '@/layouts/PublicLayout.astro';
import ProductCard from '@/components/product/ProductCard.astro';
import { supabase } from '@/lib/supabase';

// Fetch latest products for slider
const { data: newProducts } = await supabase
  .from('products')
  .select('*, brand:brands(name)')
  .order('created_at', { ascending: false })
  .limit(8);

const { data: brands } = await supabase
  .from('brands')
  .select('*')
  .limit(6);
---

<PublicLayout title="ÉCLAT - Perfumes, Maquillaje, Tratamiento" description="Descubre lo último en belleza: maquillaje, tratamiento, perfumes y cabello.">
  
  {/* 1. HERO - Premium Full-Screen Style with Image */}
  <section class="relative w-full h-[85vh] min-h-[600px] max-h-[900px] bg-gradient-to-r from-pink-300 via-pink-200 to-pink-300 overflow-hidden border-8 border-black">
    <div class="absolute inset-0 bg-gradient-to-r from-black/10 via-transparent to-transparent"></div>
    <div class="absolute inset-0 flex items-center">
      <div class="max-w-7xl mx-auto px-8 sm:px-12 lg:px-16 w-full flex flex-col lg:flex-row items-center justify-between gap-0">
        <div class="max-w-xl animate-fadeIn flex-1 relative z-10 pr-0">
          <span class="inline-block text-gray-700 text-[11px] uppercase tracking-[0.3em] mb-6 font-medium">Nueva Colección</span>
          <h1 class="text-5xl md:text-7xl text-black font-sans font-bold tracking-tight mb-6 leading-[1.1]">
            The Next<br/>Big Thing
          </h1>
          <p class="text-gray-700 text-lg md:text-xl font-light mb-10 leading-relaxed">
            Descubre la nueva revolución en cuidado de la piel. Solo en Éclat.
          </p>
          <a 
            href="/productos" 
            class="inline-block bg-black text-white px-10 py-4 font-medium text-xs uppercase tracking-[0.2em] hover:bg-white hover:text-black transition-all duration-300 shadow-button border border-black"
          >
            Descubrir Ahora
          </a>
        </div>

        {/* Product Image - Full Height */}
        <div class="hidden lg:flex items-center justify-center flex-1 h-full absolute right-0 top-0 bottom-0 w-1/2 overflow-hidden">
          <img 
            src="https://res.cloudinary.com/dmu6ttz2o/image/upload/v1769684799/eclat-beauty/hero-makeup-girl.png" 
            alt="Maquillaje Premium" 
            class="w-full h-full object-cover"
          />
        </div>
      </div>
    </div>
    
    {/* Scroll indicator */}
    <div class="absolute bottom-8 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 text-black/60 animate-bounce z-20">
      <span class="text-[10px] uppercase tracking-widest">Scroll</span>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </div>
  </section>

  {/* 2. REBAJAS SECTION - Product Grid */}
  <section class="py-16 lg:py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-end justify-between mb-12">
        <div>
          <span class="inline-block text-rose-600 text-[10px] uppercase tracking-[0.3em] mb-3 font-medium">Ofertas Exclusivas</span>
          <h2 class="text-3xl md:text-4xl font-sans font-bold text-black">Rebajas hasta -50%</h2>
        </div>
        <a href="/ofertas" class="hidden md:inline-block text-[11px] font-medium uppercase tracking-[0.15em] text-black border-b border-black pb-1 hover:text-rose-600 hover:border-rose-600 transition-colors">
          Ver todo
        </a>
      </div>

      {/* Products Grid */}
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4 md:gap-6">
        {newProducts?.slice(0, 5).map(product => (
          <ProductCard product={{
            ...product,
            brand: product.brand?.name || 'Marca'
          }} />
        ))}
      </div>

      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-8 md:hidden">
        <a href="/ofertas" class="inline-block text-[11px] font-medium uppercase tracking-[0.15em] text-black border-b border-black pb-1">
          Ver todas las ofertas
        </a>
      </div>
    </div>
    <div class="w-full h-px bg-black mt-12"></div>
  </section>

  {/* 3. NOVEDADES SECTION - Enhanced Product Banners */}
  <section class="py-20 lg:py-28 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <span class="inline-block text-gray-500 text-[10px] uppercase tracking-[0.3em] mb-3 font-medium">Lo Último</span>
        <h2 class="text-3xl md:text-5xl font-sans font-bold text-black">Novedades</h2>
        <div class="w-12 h-px bg-black mx-auto mt-6"></div>
      </div>

      {/* Banners Grid */}
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 lg:gap-12">
        {/* Banner 1: Nuevas Colecciones - Pink */}
        <a href="/productos" class="relative h-[400px] md:h-[500px] bg-gradient-to-br from-pink-300 via-pink-200 to-pink-300 overflow-hidden group border-4 border-black">
          <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
          <div class="relative z-10 h-full flex flex-col items-start justify-end p-10 md:p-14">
            <span class="inline-block text-black text-[10px] uppercase tracking-[0.3em] mb-4 font-medium">Colecciones</span>
            <h3 class="text-4xl md:text-5xl font-sans font-bold text-black mb-4">Nuevas Llegadas</h3>
            <p class="text-black/80 text-base mb-8 max-w-sm leading-relaxed">Descubre nuestras últimas colecciones con fórmulas innovadoras y colores exclusivos.</p>
            <span class="inline-block text-[11px] font-medium uppercase tracking-[0.15em] text-black border-b-2 border-black pb-1 group-hover:border-rose-600 group-hover:text-rose-600 transition-colors">
              Explorar Colección
            </span>
          </div>
        </a>

        {/* Banner 2: Bestsellers - Gray */}
        <a href="/productos" class="relative h-[400px] md:h-[500px] bg-gray-400 overflow-hidden group border-4 border-black">
          <div class="absolute inset-0 bg-gradient-to-t from-black/30 to-black/10"></div>
          <div class="relative z-10 h-full flex flex-col items-start justify-end p-10 md:p-14">
            <span class="inline-block text-white/90 text-[10px] uppercase tracking-[0.3em] mb-4 font-medium">Top Productos</span>
            <h3 class="text-4xl md:text-5xl font-sans font-bold text-white mb-4">Bestsellers</h3>
            <p class="text-white/90 text-base mb-8 max-w-sm leading-relaxed">Los productos más amados por nuestras clientes. Calidad y resultados garantizados.</p>
            <span class="inline-block bg-white text-black px-7 py-3 text-[10px] font-medium uppercase tracking-[0.15em] group-hover:bg-black group-hover:text-white transition-all duration-300 border border-white">
              Ver Bestsellers
            </span>
          </div>
        </a>
      </div>

      <a href="/productos" class="flex justify-center mt-12">
        <span class="text-[11px] font-medium uppercase tracking-[0.15em] text-black border-b border-black pb-1 hover:text-rose-600 hover:border-rose-600 transition-colors">
          Ver todos los productos
        </span>
      </a>
    </div>
    <div class="w-full h-px bg-black mt-12"></div>
  </section>

  {/* 4. TOP CATEGORIES - Featured Categories Grid */}
  <section class="py-20 lg:py-28 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-sans font-bold text-black">Top categorías</h2>
        <div class="w-12 h-px bg-black mx-auto mt-6"></div>
      </div>

      {/* Categories Grid */}
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-4 lg:gap-6">
        {[
          { name: 'Perfumes para mujer', slug: 'perfumes', color: 'from-pink-300 to-pink-200' },
          { name: 'Perfumes para hombre', slug: 'perfumes-hombre', color: 'from-blue-300 to-blue-200' },
          { name: 'Maquillaje', slug: 'maquillaje', color: 'from-rose-300 to-rose-200' },
          { name: 'Cosmética', slug: 'cosmetica', color: 'from-purple-300 to-purple-200' },
          { name: 'Cabello', slug: 'cabello', color: 'from-amber-300 to-amber-200' }
        ].map((category) => (
          <a 
            href={`/categoria/${category.slug}`}
            class={`relative h-60 md:h-80 overflow-hidden group border-4 border-black bg-gradient-to-br ${category.color}`}
          >
            <div class="absolute inset-0 bg-black/0 group-hover:bg-black/20 transition-colors duration-300"></div>
            <div class="absolute inset-0 flex items-center justify-center">
              <h3 class="text-xl md:text-2xl font-sans font-bold text-black text-center px-4 group-hover:scale-105 transition-transform duration-300">
                {category.name}
              </h3>
            </div>
            <div class="absolute bottom-0 left-0 right-0 h-1 bg-black transform origin-left scale-x-0 group-hover:scale-x-100 transition-transform duration-300"></div>
          </a>
        ))}
      </div>
    </div>
    <div class="w-full h-px bg-black mt-12"></div>
  </section>
  <section class="bg-black text-white py-20 lg:py-28 border-y-8 border-black">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <span class="inline-block text-white/50 text-[10px] uppercase tracking-[0.3em] mb-4">Descubre</span>
        <h2 class="text-3xl md:text-4xl font-sans font-bold mb-4">Marcas Estrella</h2>
        <div class="w-12 h-px bg-rose-600 mx-auto"></div>
      </div>

      <div class="grid grid-cols-2 md:grid-cols-4 gap-3 md:gap-4">
        {['Rare Beauty', 'Fenty Beauty', 'Dior', 'Charlotte Tilbury'].map((brand, i) => (
          <a 
            href={`/marcas/${brand.toLowerCase().replace(' ', '-')}`}
            class={`relative h-64 md:h-80 bg-black-soft group overflow-hidden border-2 border-white/20 hover:border-rose-600 transition-all ${i === 0 ? 'md:col-span-2 md:h-96' : ''}`}
          >
            <div class="absolute inset-0 flex items-center justify-center bg-gradient-to-b from-gray-dark/30 to-black/60">
              <span class="text-white/10 font-sans text-4xl md:text-5xl font-bold uppercase tracking-wider group-hover:text-white/5 transition-all duration-500">
                {brand}
              </span>
            </div>
            
            <div class="absolute inset-0 flex flex-col items-center justify-center p-6 opacity-100 group-hover:opacity-0 transition-opacity duration-300">
              <h3 class="text-xl md:text-2xl font-sans font-bold tracking-wide mb-3 text-center">{brand}</h3>
              <div class="w-8 h-px bg-white/30"></div>
            </div>
            
            <div class="absolute inset-0 flex flex-col items-center justify-center p-6 opacity-0 group-hover:opacity-100 transition-opacity duration-300 bg-black/40">
              <span class="text-[10px] font-medium uppercase tracking-[0.2em] border-b border-white pb-1">
                Descubrir
              </span>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* 6. EXCLUSIVES BANNER - Sophisticated with Border */}
  <section class="py-20 lg:py-28 bg-white border-y-8 border-black">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-center">
        <div class="order-2 lg:order-1">
          <span class="inline-block text-rose-600 text-[10px] uppercase tracking-[0.3em] mb-6 font-medium">Solo en Éclat</span>
          <h2 class="text-4xl md:text-5xl font-sans font-bold text-black mb-6 leading-tight">
            Éclat Collection
          </h2>
          <p class="text-gray-700 text-lg leading-relaxed mb-10 max-w-md">
            Descubre nuestra propia línea de maquillaje, diseñada para realzar tu belleza natural con fórmulas premium.
          </p>
          <a 
            href="/marcas/eclat-collection" 
            class="inline-block bg-black text-white px-10 py-4 text-xs font-medium uppercase tracking-[0.2em] hover:bg-rose-600 transition-all duration-300 border border-black"
          >
            Ver Colección
          </a>
        </div>
        <div class="order-1 lg:order-2">
          <div class="relative aspect-[4/5] bg-gray-300 overflow-hidden border-4 border-black">
            <div class="w-full h-full bg-gradient-to-br from-pink-300 to-pink-200 flex items-center justify-center">
              <span class="text-white/20 font-sans text-6xl font-bold">Éclat</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* 6. SERVICES - Minimal Icons */}
  <section class="bg-white-off py-20 lg:py-24 border-t border-gray-border">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <span class="inline-block text-gray-medium text-[10px] uppercase tracking-[0.3em] mb-4">Experiencia</span>
        <h2 class="text-3xl md:text-4xl font-serif font-light text-black">Servicios Éclat</h2>
      </div>
      
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 lg:gap-12">
        {[
          { title: 'Tarjeta Regalo', icon: '🎁', desc: 'El regalo perfecto' },
          { title: 'App Éclat', icon: '📱', desc: 'Descarga nuestra App' },
          { title: 'Brow Bar', icon: '✨', desc: 'Servicio de cejas' },
          { title: 'Maquillaje', icon: '💄', desc: 'Servicios en tienda' },
        ].map(service => (
          <a href="#" class="text-center group p-6 rounded-lg hover:bg-white hover:shadow-soft transition-all duration-300">
            <div class="text-4xl mb-5 group-hover:scale-110 transition-transform duration-300">{service.icon}</div>
            <h3 class="font-medium text-sm uppercase tracking-[0.1em] mb-2 text-black">{service.title}</h3>
            <p class="text-xs text-gray-medium">{service.desc}</p>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* 7. TRUST SIGNALS - Premium Style */}
  <section class="bg-black text-white py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 lg:gap-8">
        {[
          { title: 'Envío Gratis', desc: 'En pedidos +30€' },
          { title: 'Muestras Gratis', desc: '3 muestras en cada pedido' },
          { title: 'Devoluciones', desc: 'Gratuitas y sencillas' },
          { title: 'Click & Collect', desc: 'Recogida en tienda 2h' },
        ].map((item, i) => (
          <div class={`text-center py-4 ${i < 3 ? 'md:border-r md:border-white/10' : ''}`}>
            <p class="font-medium text-[11px] uppercase tracking-[0.15em] mb-1.5">{item.title}</p>
            <p class="text-[10px] text-white/50">{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

</PublicLayout>

<style>
  /* Hide scrollbar for Product Slider */
  .scrollbar-hide::-webkit-scrollbar {
      display: none;
  }
  .scrollbar-hide {
      -ms-overflow-style: none;  /* IE and Edge */
      scrollbar-width: none;  /* Firefox */
  }
</style>
  }
</style>
