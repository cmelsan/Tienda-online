---
import PublicLayout from '@/layouts/PublicLayout.astro';
import ProductCard from '@/components/product/ProductCard.astro';
import CountdownTimer from '@/components/islands/CountdownTimer';
import { supabase } from '@/lib/supabase';

// Fetch latest products for slider
const { data: newProducts } = await supabase
  .from('products')
  .select('*, brand:brands(name)')
  .order('created_at', { ascending: false })
  .limit(8);

const { data: brands } = await supabase
  .from('brands')
  .select('*')
  .limit(6);

// Fetch Global Settings (Offers Visibility)
const { data: offersSettings } = await supabase
  .from('app_settings')
  .select('value')
  .eq('key', 'offers_enabled')
  .single();

const offersEnabled = offersSettings?.value === true;

// Fetch Featured Offers (products for rebaja section)
const { data: featuredOffersData } = await supabase
  .from('app_settings')
  .select('value')
  .eq('key', 'featured_offers')
  .single();

const featuredOffers = featuredOffersData?.value || [];

// Fetch the featured products
let featuredProducts = [];
if (featuredOffers.length > 0) {
  const offersWithDefaults = featuredOffers.map((o: any) => ({
    id: o.id || o,
    discount: o.discount || 0
  }));
  
  const offerIds = offersWithDefaults.map((o: any) => o.id);
  
  const { data: products } = await supabase
    .from('products')
    .select('*, brand:brands(name)')
    .in('id', offerIds);
  
  // Order by the order in featuredOffers and add discount info
  featuredProducts = offersWithDefaults
    .map((offer: any) => {
      const product = products?.find((p: any) => p.id === offer.id);
      if (product) {
        return {
          ...product,
          discount: offer.discount || 0,
          discountedPrice: Math.round(product.price * (1 - (offer.discount || 0) / 100))
        };
      }
      return null;
    })
    .filter(Boolean);
}

// Fetch Flash Sales Settings
const { data: flashSaleSettings } = await supabase
  .from('app_settings')
  .select('value')
  .eq('key', 'flash_sale_enabled')
  .single();

const flashSaleEnabled = flashSaleSettings?.value === true;

// Fetch Flash Sale Products (up to 6)
const { data: flashSaleProducts } = await supabase
  .from('products')
  .select('*, brand:brands(name)')
  .eq('is_flash_sale', true)
  .limit(6);

// Get the earliest end time for countdown
const flashSaleEndTime = flashSaleProducts && flashSaleProducts.length > 0
  ? flashSaleProducts.reduce((earliest: string, product: any) => {
      if (!product.flash_sale_end_time) return earliest;
      if (!earliest) return product.flash_sale_end_time;
      return new Date(product.flash_sale_end_time) > new Date(earliest) 
        ? product.flash_sale_end_time 
        : earliest;
    }, '')
  : null;


---

<PublicLayout title="ÉCLAT - Perfumes, Maquillaje, Tratamiento" description="Descubre lo último en belleza: maquillaje, tratamiento, perfumes y cabello.">
  
  {/* 1. HERO - Minimalist Black White Pink Design */}
  <section class="relative w-full h-[85vh] min-h-[600px] max-h-[900px] bg-black overflow-hidden">
    {/* Background Image - Right Side Only */}
    <div class="absolute right-0 top-0 bottom-0 w-1/2 hidden lg:block">
      <img 
        src="https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770483149/eclat-beauty/hero-makeup-girl.jpg" 
        alt="Maquillaje Premium" 
        class="w-full h-full object-cover"
      />
      <div class="absolute inset-0 bg-gradient-to-l from-transparent to-black/40"></div>
    </div>

    {/* Content */}
    <div class="relative z-10 h-full flex items-center">
      <div class="max-w-7xl mx-auto px-8 sm:px-12 lg:px-16 w-full">
        <div class="max-w-2xl">
          <div class="h-1 w-12 bg-pink-500 mb-8"></div>
          <h1 class="text-7xl lg:text-8xl text-white font-black tracking-tighter mb-6 leading-tight">
            NUEVA<br/>COLECCIÓN
          </h1>
          <p class="text-white/70 text-lg mb-10 font-light leading-relaxed max-w-md">
            Descubre la revolucion en cuidado de la piel. Productos premium seleccionados para ti.
          </p>
          <a 
            href="/productos" 
            class="inline-flex items-center gap-3 bg-pink-500 text-black px-8 py-4 font-bold text-sm uppercase tracking-wider hover:bg-pink-600 transition-all duration-300"
          >
            Ver Colección
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      </div>
    </div>
    
    {/* Scroll indicator */}
    <div class="absolute bottom-8 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 text-pink-500 animate-bounce z-20">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </div>
  </section>

  {/* 2. EXCLUSIVE OFFER SECTION - Register CTA */}
  <section class="relative py-12 lg:py-16 bg-white overflow-hidden">
    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        {/* Subtitle */}
        <span class="inline-block text-pink-500 text-xs uppercase tracking-[0.25em] mb-3 font-bold">Oferta Especial</span>
        
        {/* Main Heading */}
        <h2 class="text-4xl lg:text-5xl font-black text-black mb-4 tracking-tighter leading-tight">
          Únete a ÉCLAT y ahorra
        </h2>
        
        {/* Discount */}
        <div class="mb-6">
          <span class="text-6xl lg:text-7xl font-black text-pink-500">20%</span>
        </div>
        
        {/* Description */}
        <p class="text-gray-700 text-sm lg:text-base max-w-2xl mx-auto mb-8 leading-relaxed font-light">
          Crea tu cuenta y obtén <span class="text-black font-semibold">20% de descuento</span> en tu primera compra.
        </p>
        
        {/* CTA Buttons */}
        <div class="flex flex-col sm:flex-row gap-3 justify-center">
          <a 
            href="/registro"
            class="inline-flex items-center gap-2 bg-pink-500 text-black px-8 py-3 font-bold text-xs uppercase tracking-wider hover:bg-pink-600 transition-all duration-300"
          >
            Crear Cuenta
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
          
          <a 
            href="/login"
            class="inline-flex items-center justify-center border border-black text-black px-8 py-3 font-bold text-xs uppercase tracking-wider hover:bg-black hover:text-white transition-all duration-300"
          >
            Ya tengo cuenta
          </a>
        </div>
        
        {/* Small Text */}
        <p class="text-gray-500 text-xs mt-10">
          Código automático incluido al registrarte. No requiere cupón.
        </p>
      </div>
    </div>
  </section>

  {/* 2. SEPARATOR BAR */}
  <div class="h-16 lg:h-20 bg-black"></div>

  {/* 3. REBAJAS SECTION - Product Grid */}
  {offersEnabled && featuredProducts.length > 0 && (
    <section class="py-8 lg:py-12 bg-gray-100 relative">
      {/* Top accent line */}
      <div class="absolute top-0 left-0 w-20 h-1 bg-pink-500"></div>
      
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-4 mb-8">
          <div>
            <span class="inline-block text-pink-500 text-[10px] uppercase tracking-[0.3em] mb-2 font-medium">Ofertas Exclusivas</span>
            <h2 class="text-3xl md:text-4xl font-sans font-black text-black mb-3">Rebajas hasta -50%</h2>
            <div class="w-12 h-1 bg-pink-500"></div>
          </div>
          <a href="/ofertas" class="hidden md:inline-block text-[11px] font-medium uppercase tracking-[0.15em] text-black border-b-2 border-pink-500 pb-2 hover:text-pink-500 transition-colors">
            Ver todo
          </a>
        </div>

        {/* Products Grid */}
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-3 md:gap-4 mb-8">
          {featuredProducts?.slice(0, 5).map((product: any) => (
            <div class="group bg-white p-3 border border-gray-200 hover:border-pink-500 hover:shadow-lg transition-all duration-300">
              <ProductCard product={{
                ...product,
                brand: product.brand?.name || 'Marca'
              }} />
            </div>
          ))}
        </div>

        <div class="flex items-center justify-center">
          <a href="/ofertas" class="inline-flex items-center gap-2 bg-pink-500 text-white px-8 py-2 font-bold text-xs uppercase tracking-wider hover:bg-pink-600 transition-all duration-300">
            Ver todas las ofertas
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      </div>
      
      {/* Bottom accent line */}
      <div class="absolute bottom-0 right-0 w-20 h-1 bg-pink-500"></div>
    </section>
  )}

  {/* SEPARATOR BAR */}
  <div class="h-16 lg:h-20 bg-black"></div>

  {/* 4. NOVEDADES SECTION - Enhanced Product Banners */}
  <section class="py-12 lg:py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <span class="inline-block text-gray-500 text-[10px] uppercase tracking-[0.35em] mb-3 font-semibold">Lo Último</span>
        <h2 class="text-4xl md:text-5xl font-sans font-black text-black tracking-tight">Novedades</h2>
        <div class="w-16 h-0.5 bg-black mx-auto mt-6"></div>
      </div>

      {/* Banners Grid */}
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 lg:gap-8">
        {/* Banner 1: Nuevas Colecciones - Pink with Image */}
        <a href="/productos" class="relative h-[450px] md:h-[520px] overflow-hidden group shadow-lg hover:shadow-2xl transition-shadow duration-500">
          {/* Background Image */}
          <div class="absolute inset-0">
            <img 
              src="https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770482268/eclat-beauty/nuevas-llegadas.jpg" 
              alt="Nuevas Llegadas"
              class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"
            />
          </div>
          
          {/* Gradient Overlay - Pink Tint */}
          <div class="absolute inset-0 bg-gradient-to-br from-pink-500/60 via-pink-400/50 to-rose-500/60 group-hover:from-pink-600/70 group-hover:via-pink-500/60 group-hover:to-rose-600/70 transition-all duration-500"></div>
          
          {/* Dark Bottom Gradient for Text Readability */}
          <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent"></div>
          
          {/* Decorative Blur Elements */}
          <div class="absolute top-0 right-0 w-80 h-80 bg-white/20 rounded-full blur-3xl -mr-40 -mt-40 group-hover:scale-125 transition-transform duration-700"></div>
          
          {/* Content */}
          <div class="relative z-10 h-full flex flex-col items-start justify-end p-8 md:p-12">
            <span class="inline-block text-white/95 text-[10px] uppercase tracking-[0.35em] mb-3 font-bold bg-white/10 px-3 py-1.5 backdrop-blur-sm">Colecciones</span>
            <h3 class="text-5xl md:text-6xl font-sans font-black text-white mb-4 leading-[0.95] tracking-tight">Nuevas<br/>Llegadas</h3>
            <p class="text-white/95 text-base md:text-lg mb-6 max-w-md leading-relaxed font-medium">Descubre nuestras últimas colecciones con fórmulas innovadoras y colores exclusivos.</p>
            <span class="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[0.2em] text-white bg-black/30 backdrop-blur-sm px-6 py-3 border-2 border-white group-hover:bg-white group-hover:text-black transition-all duration-300 transform group-hover:translate-x-2">
              Explorar Colección
              <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </span>
          </div>
          
          {/* Shine Effect */}
          <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
        </a>

        {/* Banner 2: Bestsellers - Dark with Image */}
        <a href="/productos" class="relative h-[450px] md:h-[520px] overflow-hidden group shadow-lg hover:shadow-2xl transition-shadow duration-500">
          {/* Background Image */}
          <div class="absolute inset-0">
            <img 
              src="https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770482415/eclat-beauty/best-sellers.jpg" 
              alt="Bestsellers"
              class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"
            />
          </div>
          
          {/* Gradient Overlay - Dark Elegant */}
          <div class="absolute inset-0 bg-gradient-to-br from-gray-900/80 via-slate-800/75 to-black/85 group-hover:from-gray-900/85 group-hover:via-slate-800/80 group-hover:to-black/90 transition-all duration-500"></div>
          
          {/* Accent Gradient */}
          <div class="absolute inset-0 bg-gradient-to-t from-rose-900/30 via-transparent to-transparent"></div>
          
          {/* Decorative Blur Elements */}
          <div class="absolute top-0 left-0 w-80 h-80 bg-rose-500/10 rounded-full blur-3xl -ml-40 -mt-40 group-hover:scale-125 transition-transform duration-700"></div>
          
          {/* Content */}
          <div class="relative z-10 h-full flex flex-col items-start justify-end p-8 md:p-12">
            <span class="inline-block text-white/95 text-[10px] uppercase tracking-[0.35em] mb-3 font-bold bg-white/10 px-3 py-1.5 backdrop-blur-sm">Top Productos</span>
            <h3 class="text-5xl md:text-6xl font-sans font-black text-white mb-4 leading-[0.95] tracking-tight">Best<br/>sellers</h3>
            <p class="text-white/95 text-base md:text-lg mb-6 max-w-md leading-relaxed font-medium">Los productos más amados por nuestras clientes. Calidad y resultados garantizados.</p>
            <span class="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[0.2em] bg-white text-black px-6 py-3 group-hover:bg-pink-500 group-hover:text-white transition-all duration-300 transform group-hover:translate-x-2 shadow-lg">
              Ver Bestsellers
              <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </span>
          </div>
          
          {/* Shine Effect */}
          <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/15 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
        </a>
      </div>

      <a href="/productos" class="flex justify-center mt-10">
        <span class="text-xs font-bold uppercase tracking-[0.2em] text-black border-b-2 border-black pb-1.5 hover:text-rose-600 hover:border-rose-600 transition-colors">
          Ver todos los productos
        </span>
      </a>
    </div>
  </section>

  {/* SEPARATOR BAR */}
  <div class="h-16 lg:h-20 bg-black"></div>

  <section class="bg-white">

  {/* 4. FLASH SALE SECTION - CAROUSEL */}
  {flashSaleEnabled && flashSaleProducts && flashSaleProducts.length > 0 && (
    <section class="py-16 lg:py-24 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        {/* Header with Timer */}
        <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-8 mb-12">
          <div>
            <h2 class="text-3xl md:text-4xl font-sans font-black text-black tracking-tight mb-2">
              Ofertas Flash
            </h2>
            <p class="text-gray-600 text-sm">Productos seleccionados con increíbles descuentos</p>
          </div>

          {/* Countdown Timer */}
          {flashSaleEndTime && (
            <div client:load>
              <CountdownTimer client:only="react" endTime={flashSaleEndTime} />
            </div>
          )}
        </div>

        {/* Carousel Container */}
        <div class="relative group">
          {/* Products Carousel */}
          <div id="flashSaleCarousel" class="overflow-x-auto scrollbar-hide">
            <div class="flex gap-6 pb-4">
              {flashSaleProducts.map((product: any) => {
                const discount = product.flash_sale_discount || 0;
                const discountedPrice = Math.round(product.price * (1 - discount / 100));
                
                return (
                  <a 
                    href={`/productos/${product.slug}`}
                    class="flex-none w-80 group/card bg-white border border-gray-200 hover:border-gray-400 transition-all duration-300 hover:shadow-lg overflow-hidden"
                  >
                    {/* Product Image Container */}
                    <div class="relative bg-gray-50 aspect-square overflow-hidden">
                      <img 
                        src={product.images && product.images.length > 0 ? product.images[0] : '/placeholder-product.jpg'} 
                        alt={product.name}
                        class="w-full h-full object-contain p-6 group-hover/card:scale-105 transition-transform duration-500"
                      />
                      
                      {/* Discount Badge - Top Right Corner */}
                      <div class="absolute top-3 right-3 bg-pink-500 text-white px-2 py-1 rounded text-xs font-black">
                        -{discount}%
                      </div>
                    </div>

                    {/* Product Info */}
                    <div class="p-5">
                      <p class="text-[9px] font-bold uppercase tracking-widest text-gray-500 mb-2">{product.brand?.name || 'MARCA'}</p>
                      <h3 class="text-sm font-bold text-black mb-3 line-clamp-2 leading-snug min-h-9">{product.name}</h3>
                      
                      {/* Price Section */}
                      <div class="flex items-baseline gap-2 mb-4">
                        <span class="text-base font-black text-rose-600">{(discountedPrice / 100).toFixed(2)} €</span>
                        <span class="text-xs font-semibold text-gray-400 line-through">{(product.price / 100).toFixed(2)} €</span>
                      </div>

                      <button class="w-full bg-pink-500 text-white py-2 text-[10px] font-bold uppercase tracking-wider hover:bg-pink-600 transition-colors duration-300">
                        Comprar Ahora
                      </button>
                    </div>
                  </a>
                );
              })}
            </div>
          </div>

          {/* Left Arrow */}
          <button 
            onclick="document.getElementById('flashSaleCarousel').scrollBy({left: -400, behavior: 'smooth'})"
            class="absolute left-0 top-1/2 -translate-y-1/2 -translate-x-4 z-10 bg-black hover:bg-pink-500 text-white p-3 rounded-full transition-colors duration-300 opacity-0 group-hover:opacity-100 group-hover:-translate-x-6"
            aria-label="Anterior"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
            </svg>
          </button>

          {/* Right Arrow */}
          <button 
            onclick="document.getElementById('flashSaleCarousel').scrollBy({left: 400, behavior: 'smooth'})"
            class="absolute right-0 top-1/2 -translate-y-1/2 translate-x-4 z-10 bg-black hover:bg-pink-500 text-white p-3 rounded-full transition-colors duration-300 opacity-0 group-hover:opacity-100 group-hover:translate-x-6"
            aria-label="Siguiente"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </button>
        </div>

        {/* CTA Button */}
        <div class="text-center mt-10">
          <a 
            href="/ofertas"
            class="inline-block bg-black text-white px-10 py-3 text-xs font-bold uppercase tracking-[0.2em] hover:bg-pink-500 transition-all duration-300"
          >
            Ver Todas las Ofertas
          </a>
        </div>
      </div>
    </section>
  )}

  {/* SEPARATOR BAR */}
  <div class="h-16 lg:h-20 bg-black"></div>

  <section class="bg-white py-20 lg:py-28 border-y-8 border-black">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <span class="inline-block text-pink-500 text-[10px] uppercase tracking-[0.3em] mb-4 font-medium">Descubre</span>
        <h2 class="text-4xl md:text-5xl font-sans font-bold mb-6 text-black">Marcas Estrella</h2>
        <p class="text-gray-600 text-lg max-w-2xl mx-auto mb-8">Las casas de belleza más prestigiosas del mundo, seleccionadas especialmente para ti</p>
      </div>

      {/* Full-width separator line */}
      <div class="w-screen relative left-1/2 right-1/2 -mx-[50vw] h-1 bg-pink-500 mb-16"></div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {[
          { name: 'Rare Beauty', image: 'https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770481119/eclat-beauty/brands/rare-beauty.webp' },
          { name: 'Fenty Beauty', image: 'https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770481120/eclat-beauty/brands/fenty-beauty.jpg' },
          { name: 'Dior', image: 'https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770481120/eclat-beauty/brands/dior.jpg' },
          { name: 'Charlotte Tilbury', image: 'https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770481120/eclat-beauty/brands/charlotte-tilbury.webp' }
        ].map((brand, i) => (
          <a 
            href={`/busqueda?brand=${encodeURIComponent(brand.name)}`}
            class="group relative overflow-hidden rounded-lg shadow-lg hover:shadow-2xl transition-all duration-500"
          >
            {/* Background Image */}
            <div class="absolute inset-0 overflow-hidden">
              <img 
                src={brand.image}
                alt={brand.name}
                class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/30 to-transparent"></div>
            </div>
            
            {/* Content */}
            <div class="relative h-80 flex flex-col items-center justify-end p-8 text-center">
              <div class="transform group-hover:-translate-y-4 transition-transform duration-500">
                <h3 class="text-white font-sans text-2xl md:text-3xl font-bold mb-3 tracking-wide drop-shadow-lg">
                  {brand.name}
                </h3>
                <div class="w-8 h-0.5 bg-pink-500 mx-auto mb-4 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
                <span class="inline-block text-white/80 text-xs uppercase tracking-[0.2em] font-medium opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                  Descubrir Colección
                </span>
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* SEPARATOR */}
  <div class="h-16 lg:h-20 bg-black"></div>

  {/* 6. GIFT GUIDES - Collections */}
  <section class="py-12 lg:py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      {/* Section Header */}
      <div class="text-center mb-12">
        <span class="inline-block text-pink-500 text-[10px] uppercase tracking-[0.3em] mb-4 font-medium">Colecciones Temáticas</span>
        <h2 class="text-4xl md:text-5xl font-sans font-bold text-black mb-2">Guías de Compra</h2>
        <p class="text-gray-600 text-base mt-4 max-w-2xl mx-auto">Explora nuestras colecciones cuidadosamente seleccionadas para cada necesidad</p>
      </div>

      {/* Decorative Line */}
      <div class="flex justify-center mb-12">
        <div class="w-20 h-1 bg-pink-500"></div>
      </div>

      {/* Collections Grid */}
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {/* Makeup Collection */}
        <a href="/categoria/maquillaje" class="group">
          <div class="bg-white border border-gray-200 overflow-hidden hover:border-pink-500 transition-all duration-300">
            <img src="https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770488171/eclat-beauty/collection-maquillaje.webp" alt="Maquillaje" class="aspect-square object-cover w-full h-full group-hover:scale-105 transition-transform duration-300" />
            <div class="p-6">
              <h3 class="text-lg font-bold text-black mb-2 group-hover:text-pink-500 transition-colors">Maquillaje</h3>
              <p class="text-sm text-gray-600 mb-4">Bases, sombras, labiales y mucho más</p>
              <div class="flex items-center text-pink-500 font-medium text-xs uppercase tracking-[0.1em]">
                Ver colección
                <svg class="w-4 h-4 ml-2 group-hover:translate-x-2 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </div>
            </div>
          </div>
        </a>

        {/* Hair Collection */}
        <a href="/categoria/cabello" class="group">
          <div class="bg-white border border-gray-200 overflow-hidden hover:border-pink-500 transition-all duration-300">
            <img src="https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770488172/eclat-beauty/collection-cabello.jpg" alt="Cabello" class="aspect-square object-cover w-full h-full group-hover:scale-105 transition-transform duration-300" />
            <div class="p-6">
              <h3 class="text-lg font-bold text-black mb-2 group-hover:text-pink-500 transition-colors">Cabello</h3>
              <p class="text-sm text-gray-600 mb-4">Champús, acondicionadores y tratamientos</p>
              <div class="flex items-center text-pink-500 font-medium text-xs uppercase tracking-[0.1em]">
                Ver colección
                <svg class="w-4 h-4 ml-2 group-hover:translate-x-2 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </div>
            </div>
          </div>
        </a>

        {/* Body Collection */}
        <a href="/categoria/cuerpo" class="group">
          <div class="bg-white border border-gray-200 overflow-hidden hover:border-pink-500 transition-all duration-300">
            <img src="https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770488172/eclat-beauty/collection-cuerpo.jpg" alt="Cuerpo" class="aspect-square object-cover w-full h-full group-hover:scale-105 transition-transform duration-300" />
            <div class="p-6">
              <h3 class="text-lg font-bold text-black mb-2 group-hover:text-pink-500 transition-colors">Cuerpo</h3>
              <p class="text-sm text-gray-600 mb-4">Lociones, exfoliantes y cuidado corporal</p>
              <div class="flex items-center text-pink-500 font-medium text-xs uppercase tracking-[0.1em]">
                Ver colección
                <svg class="w-4 h-4 ml-2 group-hover:translate-x-2 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </div>
            </div>
          </div>
        </a>

        {/* Perfumes Collection */}
        <a href="/categoria/perfumes" class="group">
          <div class="bg-white border border-gray-200 overflow-hidden hover:border-pink-500 transition-all duration-300">
            <img src="https://res.cloudinary.com/dmu6ttz2o/image/upload/v1770488174/eclat-beauty/collection-perfumes.jpg" alt="Perfumes" class="aspect-square object-cover w-full h-full group-hover:scale-105 transition-transform duration-300" />
            <div class="p-6">
              <h3 class="text-lg font-bold text-black mb-2 group-hover:text-pink-500 transition-colors">Perfumes</h3>
              <p class="text-sm text-gray-600 mb-4">Fragancias y aromas para cada momento</p>
              <div class="flex items-center text-pink-500 font-medium text-xs uppercase tracking-[0.1em]">
                Ver colección
                <svg class="w-4 h-4 ml-2 group-hover:translate-x-2 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </div>
            </div>
          </div>
        </a>
      </div>
    </div>
  </section>

  {/* 6. SERVICES - Minimal Icons */}
  {/* SEPARATOR */}
  <div class="h-16 lg:h-20 bg-black"></div>

  {/* SERVICES - Premium Design */}
  <section class="bg-white py-12 lg:py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <span class="inline-block text-pink-500 text-[10px] uppercase tracking-[0.3em] mb-4 font-medium">Experiencia</span>
        <h2 class="text-4xl md:text-5xl font-sans font-bold text-black">Servicios Éclat</h2>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {[
          { title: 'Tarjeta Regalo', desc: 'El regalo perfecto para cualquier ocasión' },
          { title: 'App Éclat', desc: 'Descarga nuestra aplicación exclusiva' },
          { title: 'Brow Bar', desc: 'Servicio profesional de diseño de cejas' },
          { title: 'Maquillaje', desc: 'Asesoría y servicios de maquillaje en tienda' },
        ].map((service, i) => (
          <a href="#" class="group">
            <div class="bg-white border border-gray-200 p-8 hover:border-pink-500 transition-all duration-300 relative overflow-hidden">
              {/* Icon circle */}
              <div class="absolute -right-4 -top-4 w-20 h-20 bg-pink-50 group-hover:bg-pink-100 rounded-full transition-colors duration-300 -z-10"></div>
              
              {/* Number */}
              <div class="inline-flex items-center justify-center w-10 h-10 rounded-full border-2 border-gray-300 group-hover:border-pink-500 text-gray-600 group-hover:text-pink-500 font-bold text-sm transition-all duration-300 mb-4">
                {(i + 1).toString().padStart(2, '0')}
              </div>
              
              <h3 class="text-lg font-bold text-black mb-2 group-hover:text-pink-500 transition-colors duration-300">{service.title}</h3>
              <p class="text-sm text-gray-600">{service.desc}</p>
              
              <div class="flex items-center text-pink-500 font-medium text-xs uppercase tracking-[0.1em] mt-4 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                Conocer más
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* TRUST SIGNALS - Enhanced Premium Style */}
  <section class="bg-black text-white py-10 lg:py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 lg:gap-0">
        {[
          { title: 'Envío Gratis', desc: 'En pedidos +30€', icon: '→' },
          { title: 'Muestras Gratis', desc: '3 muestras en cada pedido', icon: '✓' },
          { title: 'Devoluciones', desc: 'Gratuitas y sencillas', icon: '↩' },
          { title: 'Click & Collect', desc: 'Recogida en tienda 2h', icon: '⚡' },
        ].map((item, i) => (
          <div class={`text-center py-6 lg:py-8 px-4 group hover:bg-pink-500 transition-colors duration-300 ${i < 3 ? 'lg:border-r lg:border-white/10' : ''}`}>
            <div class="text-2xl mb-3 opacity-50 group-hover:opacity-100 transition-opacity duration-300">
              {item.icon}
            </div>
            <p class="font-medium text-[11px] uppercase tracking-[0.15em] mb-2">{item.title}</p>
            <p class="text-[10px] text-white/70 group-hover:text-white/90 transition-colors duration-300">{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

</PublicLayout>

<style>
  /* Hide scrollbar for Product Slider */
  .scrollbar-hide::-webkit-scrollbar {
      display: none;
  }
  .scrollbar-hide {
      -ms-overflow-style: none;  /* IE and Edge */
      scrollbar-width: none;  /* Firefox */
  }
</style>
  }
</style>
