---
import PublicLayout from '@/layouts/PublicLayout.astro';
import ProductCard from '@/components/product/ProductCard.astro';
import { supabase } from '@/lib/supabase';

// Fetch latest products for slider
const { data: newProducts } = await supabase
  .from('products')
  .select('*, brand:brands(name)')
  .order('created_at', { ascending: false })
  .limit(8);

const { data: brands } = await supabase
  .from('brands')
  .select('*')
  .limit(6);

// Fetch Global Settings (Offers Visibility)
const { data: offersSettings } = await supabase
  .from('app_settings')
  .select('value')
  .eq('key', 'offers_enabled')
  .single();

const offersEnabled = offersSettings?.value === true;

// Fetch featured products by category for Top Categories section
const { data: featuredPerfumesMujer } = await supabase
  .from('products')
  .select('*, brand:brands(name)')
  .eq('category', 'Perfumes')
  .limit(1)
  .single();

const { data: featuredMaquillaje } = await supabase
  .from('products')
  .select('*, brand:brands(name)')
  .eq('category', 'Maquillaje')
  .limit(1)
  .single();

const { data: featuredCosmetica } = await supabase
  .from('products')
  .select('*, brand:brands(name)')
  .eq('category', 'Tratamiento')
  .limit(1)
  .single();

const { data: featuredCabello } = await supabase
  .from('products')
  .select('*, brand:brands(name)')
  .eq('category', 'Cabello')
  .limit(1)
  .single();


const { data: featuredPerfumesHombre } = await supabase
  .from('products')
  .select('*, brand:brands(name)')
  .eq('category', 'Perfumes')
  .range(1, 1)
  .single();


---

<PublicLayout title="ÉCLAT - Perfumes, Maquillaje, Tratamiento" description="Descubre lo último en belleza: maquillaje, tratamiento, perfumes y cabello.">
  
  {/* 1. HERO - Premium Full-Screen Style with Image */}
  <section class="relative w-full h-[85vh] min-h-[600px] max-h-[900px] bg-gradient-to-r from-pink-300 via-pink-200 to-pink-300 overflow-hidden border-8 border-black">
    <div class="absolute inset-0 bg-gradient-to-r from-black/10 via-transparent to-transparent"></div>
    <div class="absolute inset-0 flex items-center">
      <div class="max-w-7xl mx-auto px-8 sm:px-12 lg:px-16 w-full flex flex-col lg:flex-row items-center justify-between gap-0">
        <div class="max-w-xl animate-fadeIn flex-1 relative z-10 pr-0">
          <span class="inline-block text-gray-700 text-[10px] uppercase tracking-[0.35em] mb-5 font-semibold opacity-80">Nueva Colección</span>
          <h1 class="text-6xl md:text-8xl text-black font-sans font-black tracking-tighter mb-7 leading-[0.95]">
            The Next<br/>Big Thing
          </h1>
          <p class="text-gray-800 text-base md:text-lg font-normal mb-12 leading-relaxed max-w-md">
            Descubre la nueva revolución en cuidado de la piel. Solo en Éclat.
          </p>
          <a 
            href="/productos" 
            class="group inline-block bg-black text-white px-12 py-5 font-bold text-xs uppercase tracking-[0.25em] hover:bg-gray-900 hover:shadow-xl transition-all duration-300 border border-black relative overflow-hidden"
          >
            <span class="relative z-10">Descubrir Ahora</span>
            <div class="absolute inset-0 bg-white transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300 origin-left"></div>
            <span class="absolute inset-0 flex items-center justify-center text-black opacity-0 group-hover:opacity-100 transition-opacity duration-300 font-bold z-20">Descubrir Ahora</span>
          </a>
        </div>

        {/* Product Image - Full Height */}
        <div class="hidden lg:flex items-center justify-center flex-1 h-full absolute right-0 top-0 bottom-0 w-1/2 overflow-hidden">
          <img 
            src="https://res.cloudinary.com/dmu6ttz2o/image/upload/v1769684799/eclat-beauty/hero-makeup-girl.png" 
            alt="Maquillaje Premium" 
            class="w-full h-full object-cover"
          />
        </div>
      </div>
    </div>
    
    {/* Scroll indicator */}
    <div class="absolute bottom-10 left-1/2 -translate-x-1/2 flex flex-col items-center gap-3 text-black/50 animate-bounce z-20">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </div>
  </section>

  {/* 2. EXCLUSIVE OFFER SECTION - Register CTA */}
  <section class="relative py-20 lg:py-32 bg-gradient-to-r from-black via-black to-black overflow-hidden">
    {/* Decorative Background Elements */}
    <div class="absolute top-0 right-0 w-96 h-96 bg-rose-600/20 rounded-full blur-3xl -mr-48 -mt-48"></div>
    <div class="absolute bottom-0 left-0 w-96 h-96 bg-pink-600/20 rounded-full blur-3xl -ml-48 -mb-48"></div>
    
    <div class="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        {/* Subtitle */}
        <span class="inline-block text-rose-400 text-[10px] uppercase tracking-[0.35em] mb-4 font-bold">Oferta Especial</span>
        
        {/* Main Heading */}
        <h2 class="text-5xl md:text-6xl lg:text-7xl font-sans font-black text-white mb-6 tracking-tight leading-[0.95]">
          Únete a ÉCLAT<br/>y ahorra
        </h2>
        
        {/* Big Discount Badge */}
        <div class="inline-block mb-8">
          <span class="text-6xl md:text-8xl font-black bg-gradient-to-r from-pink-500 to-rose-500 bg-clip-text text-transparent">20%</span>
        </div>
        
        {/* Description */}
        <p class="text-white/80 text-lg md:text-xl max-w-2xl mx-auto mb-12 leading-relaxed font-light">
          Crea tu cuenta hoy y recibe un descuento exclusivo de <span class="font-bold text-white">20% de descuento</span> en tu primera compra. Más beneficios esperan por ti.
        </p>
        
        {/* Benefits List */}
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-8 mb-12 max-w-3xl mx-auto">

        </div>
        
        {/* CTA Buttons */}
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="/registro"
            class="group relative inline-flex items-center justify-center bg-gradient-to-r from-rose-600 to-pink-600 text-white px-10 py-5 font-bold text-sm uppercase tracking-[0.15em] overflow-hidden shadow-2xl hover:shadow-3xl transition-all duration-300 hover:-translate-y-1"
          >
            <span class="relative z-10">Crear Cuenta Ahora</span>
            <div class="absolute inset-0 bg-gradient-to-r from-pink-600 to-rose-600 transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300 origin-left"></div>
          </a>
          
          <a 
            href="/login"
            class="group relative inline-flex items-center justify-center border-2 border-white text-white px-10 py-4 font-bold text-sm uppercase tracking-[0.15em] hover:bg-white hover:text-black transition-all duration-300"
          >
            Ya tengo cuenta
          </a>
        </div>
        
        {/* Small Text */}
        <p class="text-white/50 text-xs mt-10">
          Código automático incluido al registrarte. No requiere cupón.
        </p>
      </div>
    </div>
  </section>

  {/* 3. REBAJAS SECTION - Product Grid */}
  {offersEnabled && (
  <section class="py-16 lg:py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-end justify-between mb-12">
        <div>
          <span class="inline-block text-rose-600 text-[10px] uppercase tracking-[0.3em] mb-3 font-medium">Ofertas Exclusivas</span>
          <h2 class="text-3xl md:text-4xl font-sans font-bold text-black">Rebajas hasta -50%</h2>
        </div>
        <a href="/ofertas" class="hidden md:inline-block text-[11px] font-medium uppercase tracking-[0.15em] text-black border-b border-black pb-1 hover:text-rose-600 hover:border-rose-600 transition-colors">
          Ver todo
        </a>
      </div>

      {/* Products Grid */}
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4 md:gap-6">
        {newProducts?.slice(0, 5).map(product => (
          <ProductCard product={{
            ...product,
            brand: product.brand?.name || 'Marca'
          }} />
        ))}
      </div>

      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-8 md:hidden">
        <a href="/ofertas" class="inline-block text-[11px] font-medium uppercase tracking-[0.15em] text-black border-b border-black pb-1">
          Ver todas las ofertas
        </a>
      </div>
    </div>
    <div class="w-full h-px bg-black mt-12"></div>
  </section>
  )}

  {/* 4. NOVEDADES SECTION - Enhanced Product Banners */}
  <section class="py-12 lg:py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <span class="inline-block text-gray-500 text-[10px] uppercase tracking-[0.35em] mb-3 font-semibold">Lo Último</span>
        <h2 class="text-4xl md:text-5xl font-sans font-black text-black tracking-tight">Novedades</h2>
        <div class="w-16 h-0.5 bg-black mx-auto mt-6"></div>
      </div>

      {/* Banners Grid */}
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 lg:gap-8">
        {/* Banner 1: Nuevas Colecciones - Pink with Image */}
        <a href="/productos" class="relative h-[450px] md:h-[520px] overflow-hidden group shadow-lg hover:shadow-2xl transition-shadow duration-500">
          {/* Background Image */}
          <div class="absolute inset-0">
            <img 
              src="https://images.unsplash.com/photo-1596462502278-27bfdc403348?w=800&q=80" 
              alt="Nuevas Llegadas"
              class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"
            />
          </div>
          
          {/* Gradient Overlay - Pink Tint */}
          <div class="absolute inset-0 bg-gradient-to-br from-pink-500/60 via-pink-400/50 to-rose-500/60 group-hover:from-pink-600/70 group-hover:via-pink-500/60 group-hover:to-rose-600/70 transition-all duration-500"></div>
          
          {/* Dark Bottom Gradient for Text Readability */}
          <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent"></div>
          
          {/* Decorative Blur Elements */}
          <div class="absolute top-0 right-0 w-80 h-80 bg-white/20 rounded-full blur-3xl -mr-40 -mt-40 group-hover:scale-125 transition-transform duration-700"></div>
          
          {/* Content */}
          <div class="relative z-10 h-full flex flex-col items-start justify-end p-8 md:p-12">
            <span class="inline-block text-white/95 text-[10px] uppercase tracking-[0.35em] mb-3 font-bold bg-white/10 px-3 py-1.5 backdrop-blur-sm">Colecciones</span>
            <h3 class="text-5xl md:text-6xl font-sans font-black text-white mb-4 leading-[0.95] tracking-tight">Nuevas<br/>Llegadas</h3>
            <p class="text-white/95 text-base md:text-lg mb-6 max-w-md leading-relaxed font-medium">Descubre nuestras últimas colecciones con fórmulas innovadoras y colores exclusivos.</p>
            <span class="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[0.2em] text-white bg-black/30 backdrop-blur-sm px-6 py-3 border-2 border-white group-hover:bg-white group-hover:text-black transition-all duration-300 transform group-hover:translate-x-2">
              Explorar Colección
              <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </span>
          </div>
          
          {/* Shine Effect */}
          <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
        </a>

        {/* Banner 2: Bestsellers - Dark with Image */}
        <a href="/productos" class="relative h-[450px] md:h-[520px] overflow-hidden group shadow-lg hover:shadow-2xl transition-shadow duration-500">
          {/* Background Image */}
          <div class="absolute inset-0">
            <img 
              src="https://images.unsplash.com/photo-1522335789203-aabd1fc54bc9?w=800&q=80" 
              alt="Bestsellers"
              class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"
            />
          </div>
          
          {/* Gradient Overlay - Dark Elegant */}
          <div class="absolute inset-0 bg-gradient-to-br from-gray-900/80 via-slate-800/75 to-black/85 group-hover:from-gray-900/85 group-hover:via-slate-800/80 group-hover:to-black/90 transition-all duration-500"></div>
          
          {/* Accent Gradient */}
          <div class="absolute inset-0 bg-gradient-to-t from-rose-900/30 via-transparent to-transparent"></div>
          
          {/* Decorative Blur Elements */}
          <div class="absolute top-0 left-0 w-80 h-80 bg-rose-500/10 rounded-full blur-3xl -ml-40 -mt-40 group-hover:scale-125 transition-transform duration-700"></div>
          
          {/* Content */}
          <div class="relative z-10 h-full flex flex-col items-start justify-end p-8 md:p-12">
            <span class="inline-block text-white/95 text-[10px] uppercase tracking-[0.35em] mb-3 font-bold bg-white/10 px-3 py-1.5 backdrop-blur-sm">Top Productos</span>
            <h3 class="text-5xl md:text-6xl font-sans font-black text-white mb-4 leading-[0.95] tracking-tight">Best<br/>sellers</h3>
            <p class="text-white/95 text-base md:text-lg mb-6 max-w-md leading-relaxed font-medium">Los productos más amados por nuestras clientes. Calidad y resultados garantizados.</p>
            <span class="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[0.2em] bg-white text-black px-6 py-3 group-hover:bg-rose-600 group-hover:text-white transition-all duration-300 transform group-hover:translate-x-2 shadow-lg">
              Ver Bestsellers
              <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </span>
          </div>
          
          {/* Shine Effect */}
          <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/15 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
        </a>
      </div>

      <a href="/productos" class="flex justify-center mt-10">
        <span class="text-xs font-bold uppercase tracking-[0.2em] text-black border-b-2 border-black pb-1.5 hover:text-rose-600 hover:border-rose-600 transition-colors">
          Ver todos los productos
        </span>
      </a>
    </div>
    <div class="w-full h-px bg-gray-200 mt-12"></div>
  </section>

  {/* 4. TOP CATEGORIES - Featured Products by Category */}
  <section class="py-20 lg:py-28 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <span class="inline-block text-gray-500 text-[10px] uppercase tracking-[0.35em] mb-3 font-semibold">Explora</span>
        <h2 class="text-4xl md:text-5xl font-sans font-black text-black tracking-tight">Top categorías</h2>
        <div class="w-16 h-0.5 bg-black mx-auto mt-6"></div>
      </div>

      {/* Products Grid */}
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4 lg:gap-5">
        {/* Perfumes Mujer */}
        {featuredPerfumesMujer && (
          <a 
            href={`/productos/${featuredPerfumesMujer.slug}`}
            class="group relative bg-white border-2 border-black hover:shadow-xl transition-all duration-300"
          >
            {/* Badge */}
            <div class="absolute top-3 left-3 bg-black text-white px-3 py-1 text-[9px] font-bold uppercase tracking-wider z-10">
              Perfumes
            </div>
            
            {/* Wishlist Heart */}
            <button class="absolute top-3 right-3 w-8 h-8 bg-white border border-black flex items-center justify-center hover:bg-black hover:text-white transition-colors z-10">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
              </svg>
            </button>

            {/* Product Image */}
            <div class="aspect-square bg-gray-50 overflow-hidden">
              <img 
                src={featuredPerfumesMujer.image_url} 
                alt={featuredPerfumesMujer.name}
                class="w-full h-full object-contain p-4 group-hover:scale-110 transition-transform duration-500"
              />
            </div>

            {/* Product Info */}
            <div class="p-4 border-t-2 border-black">
              <p class="text-[10px] font-bold uppercase tracking-wider text-gray-600 mb-1">{featuredPerfumesMujer.brand?.name || 'Marca'}</p>
              <h3 class="text-sm font-bold text-black mb-2 line-clamp-2 leading-tight">{featuredPerfumesMujer.name}</h3>
              <p class="text-lg font-black text-black mb-3">{featuredPerfumesMujer.price.toFixed(2)} €</p>
              <button class="w-full bg-white border-2 border-black text-black py-2 text-[10px] font-bold uppercase tracking-wider hover:bg-black hover:text-white transition-colors">
                Descubrir
              </button>
            </div>
          </a>
        )}

        {/* Perfumes Hombre */}
        {featuredPerfumesHombre && (
          <a 
            href={`/productos/${featuredPerfumesHombre.slug}`}
            class="group relative bg-white border-2 border-black hover:shadow-xl transition-all duration-300"
          >
            <div class="absolute top-3 left-3 bg-black text-white px-3 py-1 text-[9px] font-bold uppercase tracking-wider z-10">
              Hombre
            </div>
            <button class="absolute top-3 right-3 w-8 h-8 bg-white border border-black flex items-center justify-center hover:bg-black hover:text-white transition-colors z-10">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
              </svg>
            </button>
            <div class="aspect-square bg-gray-50 overflow-hidden">
              <img 
                src={featuredPerfumesHombre.image_url} 
                alt={featuredPerfumesHombre.name}
                class="w-full h-full object-contain p-4 group-hover:scale-110 transition-transform duration-500"
              />
            </div>
            <div class="p-4 border-t-2 border-black">
              <p class="text-[10px] font-bold uppercase tracking-wider text-gray-600 mb-1">{featuredPerfumesHombre.brand?.name || 'Marca'}</p>
              <h3 class="text-sm font-bold text-black mb-2 line-clamp-2 leading-tight">{featuredPerfumesHombre.name}</h3>
              <p class="text-lg font-black text-black mb-3">{featuredPerfumesHombre.price.toFixed(2)} €</p>
              <button class="w-full bg-white border-2 border-black text-black py-2 text-[10px] font-bold uppercase tracking-wider hover:bg-black hover:text-white transition-colors">
                Descubrir
              </button>
            </div>
          </a>
        )}

        {/* Maquillaje */}
        {featuredMaquillaje && (
          <a 
            href={`/productos/${featuredMaquillaje.slug}`}
            class="group relative bg-white border-2 border-black hover:shadow-xl transition-all duration-300"
          >
            <div class="absolute top-3 left-3 bg-black text-white px-3 py-1 text-[9px] font-bold uppercase tracking-wider z-10">
              Maquillaje
            </div>
            <button class="absolute top-3 right-3 w-8 h-8 bg-white border border-black flex items-center justify-center hover:bg-black hover:text-white transition-colors z-10">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
              </svg>
            </button>
            <div class="aspect-square bg-gray-50 overflow-hidden">
              <img 
                src={featuredMaquillaje.image_url} 
                alt={featuredMaquillaje.name}
                class="w-full h-full object-contain p-4 group-hover:scale-110 transition-transform duration-500"
              />
            </div>
            <div class="p-4 border-t-2 border-black">
              <p class="text-[10px] font-bold uppercase tracking-wider text-gray-600 mb-1">{featuredMaquillaje.brand?.name || 'Marca'}</p>
              <h3 class="text-sm font-bold text-black mb-2 line-clamp-2 leading-tight">{featuredMaquillaje.name}</h3>
              <p class="text-lg font-black text-black mb-3">{featuredMaquillaje.price.toFixed(2)} €</p>
              <button class="w-full bg-white border-2 border-black text-black py-2 text-[10px] font-bold uppercase tracking-wider hover:bg-black hover:text-white transition-colors">
                Descubrir
              </button>
            </div>
          </a>
        )}

        {/* Cosmética/Tratamiento */}
        {featuredCosmetica && (
          <a 
            href={`/productos/${featuredCosmetica.slug}`}
            class="group relative bg-white border-2 border-black hover:shadow-xl transition-all duration-300"
          >
            <div class="absolute top-3 left-3 bg-black text-white px-3 py-1 text-[9px] font-bold uppercase tracking-wider z-10">
              Tratamiento
            </div>
            <button class="absolute top-3 right-3 w-8 h-8 bg-white border border-black flex items-center justify-center hover:bg-black hover:text-white transition-colors z-10">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
              </svg>
            </button>
            <div class="aspect-square bg-gray-50 overflow-hidden">
              <img 
                src={featuredCosmetica.image_url} 
                alt={featuredCosmetica.name}
                class="w-full h-full object-contain p-4 group-hover:scale-110 transition-transform duration-500"
              />
            </div>
            <div class="p-4 border-t-2 border-black">
              <p class="text-[10px] font-bold uppercase tracking-wider text-gray-600 mb-1">{featuredCosmetica.brand?.name || 'Marca'}</p>
              <h3 class="text-sm font-bold text-black mb-2 line-clamp-2 leading-tight">{featuredCosmetica.name}</h3>
              <p class="text-lg font-black text-black mb-3">{featuredCosmetica.price.toFixed(2)} €</p>
              <button class="w-full bg-white border-2 border-black text-black py-2 text-[10px] font-bold uppercase tracking-wider hover:bg-black hover:text-white transition-colors">
                Descubrir
              </button>
            </div>
          </a>
        )}

        {/* Cabello */}
        {featuredCabello && (
          <a 
            href={`/productos/${featuredCabello.slug}`}
            class="group relative bg-white border-2 border-black hover:shadow-xl transition-all duration-300"
          >
            <div class="absolute top-3 left-3 bg-black text-white px-3 py-1 text-[9px] font-bold uppercase tracking-wider z-10">
              Cabello
            </div>
            <button class="absolute top-3 right-3 w-8 h-8 bg-white border border-black flex items-center justify-center hover:bg-black hover:text-white transition-colors z-10">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
              </svg>
            </button>
            <div class="aspect-square bg-gray-50 overflow-hidden">
              <img 
                src={featuredCabello.image_url} 
                alt={featuredCabello.name}
                class="w-full h-full object-contain p-4 group-hover:scale-110 transition-transform duration-500"
              />
            </div>
            <div class="p-4 border-t-2 border-black">
              <p class="text-[10px] font-bold uppercase tracking-wider text-gray-600 mb-1">{featuredCabello.brand?.name || 'Marca'}</p>
              <h3 class="text-sm font-bold text-black mb-2 line-clamp-2 leading-tight">{featuredCabello.name}</h3>
              <p class="text-lg font-black text-black mb-3">{featuredCabello.price.toFixed(2)} €</p>
              <button class="w-full bg-white border-2 border-black text-black py-2 text-[10px] font-bold uppercase tracking-wider hover:bg-black hover:text-white transition-colors">
                Descubrir
              </button>
            </div>
          </a>
        )}
      </div>
    </div>
    <div class="w-full h-px bg-gray-200 mt-16"></div>
  </section>
  <section class="bg-black text-white py-20 lg:py-28 border-y-8 border-black">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <span class="inline-block text-white/50 text-[10px] uppercase tracking-[0.3em] mb-4">Descubre</span>
        <h2 class="text-3xl md:text-4xl font-sans font-bold mb-4">Marcas Estrella</h2>
        <div class="w-12 h-px bg-rose-600 mx-auto"></div>
      </div>

      <div class="grid grid-cols-2 md:grid-cols-4 gap-3 md:gap-4">
        {['Rare Beauty', 'Fenty Beauty', 'Dior', 'Charlotte Tilbury'].map((brand, i) => (
          <a 
            href={`/marcas/${brand.toLowerCase().replace(' ', '-')}`}
            class={`relative h-64 md:h-80 bg-black-soft group overflow-hidden border-2 border-white/20 hover:border-rose-600 transition-all ${i === 0 ? 'md:col-span-2 md:h-96' : ''}`}
          >
            <div class="absolute inset-0 flex items-center justify-center bg-gradient-to-b from-gray-dark/30 to-black/60">
              <span class="text-white/10 font-sans text-4xl md:text-5xl font-bold uppercase tracking-wider group-hover:text-white/5 transition-all duration-500">
                {brand}
              </span>
            </div>
            
            <div class="absolute inset-0 flex flex-col items-center justify-center p-6 opacity-100 group-hover:opacity-0 transition-opacity duration-300">
              <h3 class="text-xl md:text-2xl font-sans font-bold tracking-wide mb-3 text-center">{brand}</h3>
              <div class="w-8 h-px bg-white/30"></div>
            </div>
            
            <div class="absolute inset-0 flex flex-col items-center justify-center p-6 opacity-0 group-hover:opacity-100 transition-opacity duration-300 bg-black/40">
              <span class="text-[10px] font-medium uppercase tracking-[0.2em] border-b border-white pb-1">
                Descubrir
              </span>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* 6. EXCLUSIVES BANNER - Sophisticated with Border */}
  <section class="py-20 lg:py-28 bg-white border-y-8 border-black">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-center">
        <div class="order-2 lg:order-1">
          <span class="inline-block text-rose-600 text-[10px] uppercase tracking-[0.3em] mb-6 font-medium">Solo en Éclat</span>
          <h2 class="text-4xl md:text-5xl font-sans font-bold text-black mb-6 leading-tight">
            Éclat Collection
          </h2>
          <p class="text-gray-700 text-lg leading-relaxed mb-10 max-w-md">
            Descubre nuestra propia línea de maquillaje, diseñada para realzar tu belleza natural con fórmulas premium.
          </p>
          <a 
            href="/marcas/eclat-collection" 
            class="inline-block bg-black text-white px-10 py-4 text-xs font-medium uppercase tracking-[0.2em] hover:bg-rose-600 transition-all duration-300 border border-black"
          >
            Ver Colección
          </a>
        </div>
        <div class="order-1 lg:order-2">
          <div class="relative aspect-[4/5] bg-gray-300 overflow-hidden border-4 border-black">
            <div class="w-full h-full bg-gradient-to-br from-pink-300 to-pink-200 flex items-center justify-center">
              <span class="text-white/20 font-sans text-6xl font-bold">Éclat</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* 6. SERVICES - Minimal Icons */}
  <section class="bg-white-off py-20 lg:py-24 border-t border-gray-border">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <span class="inline-block text-gray-medium text-[10px] uppercase tracking-[0.3em] mb-4">Experiencia</span>
        <h2 class="text-3xl md:text-4xl font-sans font-light text-black">Servicios Éclat</h2>
      </div>
      
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 lg:gap-12">
        {[
          { title: 'Tarjeta Regalo', icon: '🎁', desc: 'El regalo perfecto' },
          { title: 'App Éclat', icon: '📱', desc: 'Descarga nuestra App' },
          { title: 'Brow Bar', icon: '✨', desc: 'Servicio de cejas' },
          { title: 'Maquillaje', icon: '💄', desc: 'Servicios en tienda' },
        ].map(service => (
          <a href="#" class="text-center group p-6 rounded-lg hover:bg-white hover:shadow-soft transition-all duration-300">
            <div class="text-4xl mb-5 group-hover:scale-110 transition-transform duration-300">{service.icon}</div>
            <h3 class="font-medium text-sm uppercase tracking-[0.1em] mb-2 text-black">{service.title}</h3>
            <p class="text-xs text-gray-medium">{service.desc}</p>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* 7. TRUST SIGNALS - Premium Style */}
  <section class="bg-black text-white py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 lg:gap-8">
        {[
          { title: 'Envío Gratis', desc: 'En pedidos +30€' },
          { title: 'Muestras Gratis', desc: '3 muestras en cada pedido' },
          { title: 'Devoluciones', desc: 'Gratuitas y sencillas' },
          { title: 'Click & Collect', desc: 'Recogida en tienda 2h' },
        ].map((item, i) => (
          <div class={`text-center py-4 ${i < 3 ? 'md:border-r md:border-white/10' : ''}`}>
            <p class="font-medium text-[11px] uppercase tracking-[0.15em] mb-1.5">{item.title}</p>
            <p class="text-[10px] text-white/50">{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

</PublicLayout>

<style>
  /* Hide scrollbar for Product Slider */
  .scrollbar-hide::-webkit-scrollbar {
      display: none;
  }
  .scrollbar-hide {
      -ms-overflow-style: none;  /* IE and Edge */
      scrollbar-width: none;  /* Firefox */
  }
</style>
  }
</style>
